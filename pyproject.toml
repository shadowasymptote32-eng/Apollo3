[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "apollo3"
version = "0.5.0"
description = "Apollo3 Distributed Cognitive Runtime"

[tool.setuptools]
packages = ["apollo3"]
